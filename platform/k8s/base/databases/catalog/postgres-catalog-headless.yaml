apiVersion: v1
kind: Service
metadata:
  name: postgres-catalog-headless
  namespace: atlas
spec:
  clusterIP: None
  selector:
    app: postgres-catalog
  ports:
  - name: postgres
    port: 5432
    targetPort: 5432