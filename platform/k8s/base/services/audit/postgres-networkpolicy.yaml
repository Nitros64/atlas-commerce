apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: postgres-audit-netpol
  namespace: atlas
spec:
  podSelector:
    matchLabels:
      app: postgres-audit
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              app: audit
      ports:
        - protocol: TCP
          port: 5432
