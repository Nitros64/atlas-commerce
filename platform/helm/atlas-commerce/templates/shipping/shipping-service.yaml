{{- if .Values.shipping.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.shipping.service.name }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ .Values.shipping.labels.app }}
spec:
  type: {{ .Values.shipping.service.type }}
  selector:
    app: {{ .Values.shipping.labels.app }}
  ports:
    - name: http
      port: {{ .Values.shipping.service.port }}
      targetPort: {{ .Values.shipping.service.targetPort }}
{{- end }}