apiVersion: v1
kind: Service
metadata:
  name: postgres-svc
  namespace: atlas
spec:
  clusterIP: None
  selector:
    app: postgres-auth
  ports:
  - name: postgres
    port: 5432
    targetPort: 5432