apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: atlas
  labels:
    app: order
spec:
  type: ClusterIP
  selector:
    app: order
  ports:
  - name: http
    port: 8083
    targetPort: 8083