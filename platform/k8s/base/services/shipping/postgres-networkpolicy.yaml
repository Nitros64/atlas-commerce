apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: postgres-shipping-netpol
  namespace: atlas
spec:
  podSelector:
    matchLabels:
      app: postgres-shipping
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              app: shipping

        - podSelector:
            matchLabels:
              app: postgres-backup      
      ports:
        - protocol: TCP
          port: 5432
