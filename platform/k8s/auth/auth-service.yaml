apiVersion: v1
kind: Service
metadata:
  labels:
    app: auth
    tier: backend
  name: auth-service
  namespace: atlas
spec:
  selector:
    app: auth
  ports:
  - name: http
    port: 80
    targetPort: 8081
    protocol: TCP
  type: ClusterIP
