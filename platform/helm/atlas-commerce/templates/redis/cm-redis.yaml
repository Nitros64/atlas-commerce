{{- if .Values.redis.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: cm-redis
  namespace: {{ .Release.Namespace }}
data:
  SPRING_DATA_REDIS_HOST: {{ .Values.redis.name | quote }}
  SPRING_DATA_REDIS_PORT: {{ .Values.redis.service.port | quote }}
{{- end }}