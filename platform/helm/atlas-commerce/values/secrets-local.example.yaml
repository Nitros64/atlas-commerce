# Non-sensitive values used to validate the local Helm profile in CI.
# Copy to secrets-local.yaml and replace the value for an actual local deploy.

secrets:
  jwt:
    value: ci-placeholder-not-a-secret
