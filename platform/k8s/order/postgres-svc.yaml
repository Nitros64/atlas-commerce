apiVersion: v1
kind: Service
metadata:
  name: postgres-order
  namespace: atlas
spec:
  clusterIP: None
  selector:
    app: postgres-order
  ports:
  - name: postgres
    port: 5432
    targetPort: 5432