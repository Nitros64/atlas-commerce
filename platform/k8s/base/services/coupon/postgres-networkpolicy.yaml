apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: postgres-coupon-netpol
  namespace: atlas
spec:
  podSelector:
    matchLabels:
      app: postgres-coupon
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              app: coupon
        - podSelector:
            matchLabels:
              app: postgres-backup      
      ports:
        - protocol: TCP
          port: 5432